import ImageShow from './ImageShow'

function ImageList(){
    cost renderImages = images.map((image) => {
        retur <ImageShow key={image.id} image={image}/> 
    }) 
        <div>
            {renderImages}
        </div>
    
    } 

    export default ImageList  